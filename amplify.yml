version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - corepack prepare yarn@stable --activate
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
